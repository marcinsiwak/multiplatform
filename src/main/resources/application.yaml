ktor:
    application:
        modules:
            - pl.msiwak.ApplicationKt.module
    deployment:
        port: 8080
storage:
    jdbcURL: "jdbc:postgresql://localhost:5432/postgres"
    user: "postgres"
    password: "password"
